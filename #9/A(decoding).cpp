#include <iostream>

using namespace std;

char a[10010];
char b[20020];

int main()
{
	//freopen("game.in", "r", stdin);
	//freopen("game.out", "w", stdout);
	int K;
	while( (input = getchar()) != '0 ' && input != '\n' && input != EOF)
	{
		
	}
	
}
